# 2026-07-10 接手查缺补漏

## 背景

接手 codex 未完成的 Harness Armor 工作。对已完成的四个阶段（基础引擎、七个 Skills、安装器、测试/Evals/CI）做全面审查与查缺补漏。

## 完成内容

### 修复阻断问题

- 移除 `docs/development-log/goal-objective.md` 第一行指向仓库外绝对路径的 `skill-creator` 链接，消除 `check_references.py` 报告的 `escapes-root` 断链，恢复 45/45 测试通过。

### 触发 Evals 补强（中优先级）

- 目标 L821 要求覆盖 `/harness` 正确路由，但原触发语料只有 Codex 风格 `$harness-*`，缺少 Claude Code 风格 `/harness-*`。
- 在 `shared/evals/trigger-evals.json` 新增 14 条 `/harness-*` 显式调用用例（7 Skill × en/zh-CN）。
- 在 `tests/evals/evals.test.js` 新增测试，断言每个 Skill 的 explicit 语料同时覆盖 `/` 斜杠和 `$` 美元两种调用风格。

### 健康检查维度拆分（中优先级）

- 目标 L549-550 将"安全性"和"Harness 是否含有虚构结论"列为两个独立维度，原实现合并为单一 `safety-and-nonfiction`（15 维度）。
- 将 `shared/evals/health-dimensions.json`、`shared/scripts/harness_armor/analysis.py` 的 `HEALTH_WEIGHTS` 与 `checks` 拆分为 `safety`（权重 2）和 `nonfiction`（权重 2），总权重仍为 100，维度数变为 16。
- 在 `skills/harness-check/SKILL.md` step 7 checklist 显式列出安全性检查项（密钥暴露、破坏性脚本、prompt-injection、不安全变更边界）。

### 安装测试补强（中优先级）

- 目标 L844-849 要求验证每个客户端的实际安装，原测试仅对 `custom` 目标执行实际文件操作，其他客户端仅测试路径解析。
- 在 `tests/installation/installer.test.js` 新增 `claude-user` 和 `codex-user` 的完整 install → relocated 脚本 `--help` → doctor → uninstall 冒烟测试，使用隔离 `--home` 临时目录。

### 代码清理（低优先级）

- 删除 `shared/scripts/harness_armor/common.py` 中未使用的 `import subprocess`，避免在"不调用外部模型"约束下引起审查误解。

### 文档同步

- 同步更新 `README.md` 与 `README.zh-CN.md` 中的本地测试数量（45 → 47）。
- 修复 `goal-objective.md` 缺失的末尾换行符。

## 验证证据

- `npm test`：47/47 通过，失败 0，跳过 0。
- `npm run lint:markdown`：通过，Markdown 编码/结尾和本地引用均有效。
- `npm run test:installation`：10/10 通过，含新增 claude-user/codex-user 冒烟测试。
- 健康度维度拆分后 `score_harness_health.py` 仍输出 `max_score: 100`，重复文档不增加分数的元 metamorphic 测试通过。

## 审查结论

七个 Skills 整体质量高，与目标文档要求高度吻合。本轮审查共发现 3 中 + 7 低优先级问题，无阻断（除初始断链）。已修复全部中优先级问题和部分低优先级问题。剩余低优先级问题：

- `harness-init` 的 `docs/decisions/` 和 `docs/development-log/` 采用条件创建而非默认提案集（措辞性，可接受）。
- `package.json` 的 `<owner>` 占位符（发布前人工步骤，开发日志已说明）。
- TRAE 缺少 `trae-user`（需确认 TRAE 是否支持用户级 Skills 目录）。
- 工作流 Evals 缺少独立的"授权更新时不覆盖用户文件"用例（安装器层面已覆盖）。

## 人工验收步骤

1. 运行 `npm test`，确认测试数 47、失败 0、跳过 0。
2. 查看 `shared/evals/trigger-evals.json`，确认每个 Skill 都有 `/harness-*` 和 `$harness-*` 两种显式调用用例。
3. 运行 `python3 shared/scripts/score_harness_health.py .`，确认输出 16 个维度且 `max_score` 为 100。
4. 查看 `skills/harness-check/SKILL.md` step 7，确认安全性作为独立检查项列出。
5. 运行 `npm run test:installation`，确认 claude-user 和 codex-user 的实际安装/卸载冒烟测试通过。
6. 确认 `shared/scripts/harness_armor/common.py` 不再导入 `subprocess`。
