# 项目智能体入口

开始任务前依次阅读 `docs/HARNESS.md`、`docs/TASK_STATE.md`、
`docs/ARCHITECTURE.md` 和 `docs/VERIFICATION.md`。产品事实位于
`docs/product/PRD_v0.1.md`。

只修改任务需要的文件，不得覆盖用户未提交的修改。代码变更后运行
`npm run lint` 和 `npm run test`，未运行的检查不得声称通过。

