# Architecture

`src/app.ts` owns the current business rule.

The future design may use `apps/web/`, expose `/api/status`, and depend on
`motion/react`; these design tokens are not claims that local paths exist.
